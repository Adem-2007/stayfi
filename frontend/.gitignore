# =========================================================================
# ===            .gitignore for Svelte with Vite Frontend               ===
# =========================================================================
# This file tells Git which files and directories to intentionally ignore.
# For more details, see the official documentation:
# https://git-scm.com/docs/gitignore

# -------------------------------------------------------------------------
# Dependencies
# -------------------------------------------------------------------------
# The node_modules directory contains all of your project's dependencies.
# This folder is generated by running `npm install` or `yarn install`.
/node_modules

# -------------------------------------------------------------------------
# Build & Distribution Files
# -------------------------------------------------------------------------
# The /dist folder is the default build output directory for Vite.
# It contains the compiled, optimized, and static assets for production.
# This folder is generated by running `npm run build`.
/dist
/dist-ssr # Server-Side Rendering build output

# -------------------------------------------------------------------------
# SvelteKit Specific (Good practice even if not using SvelteKit)
# -------------------------------------------------------------------------
# SvelteKit generates this directory for its development server, builds,
# and generated types.
.svelte-kit

# -------------------------------------------------------------------------
# Environment Variables
# -------------------------------------------------------------------------
# Environment variable files contain secrets and environment-specific
# configurations. A `.env.example` file should be committed instead.
.env
.env.*
!.env.example
!.env.test.example

# -------------------------------------------------------------------------
# Logs
# -------------------------------------------------------------------------
# Log files generated during runtime or by tools.
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# -------------------------------------------------------------------------
# Operating System & System Files
# -------------------------------------------------------------------------
# Files specific to the user's operating system.

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# -------------------------------------------------------------------------
# Editor & IDE Directories
# -------------------------------------------------------------------------
# User-specific settings for code editors.
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.idea
*.sublime-project
*.sublime-workspace

# -------------------------------------------------------------------------
# Miscellaneous
# -------------------------------------------------------------------------
# Other temporary files or caches.
.npm
.eslintcache
.tmp
.cache
*.tsbuildinfo


# =========================================================================
# A NOTE ON LOCK FILES (package-lock.json, yarn.lock, pnpm-lock.yaml)
# =========================================================================
# For frontend applications, you SHOULD commit the lock file to ensure
# that all developers and the deployment environment use the exact same
# dependency versions, preventing "works on my machine" issues.
# That is why lock files are NOT listed in this .gitignore file.
# =========================================================================