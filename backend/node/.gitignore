# =========================================================================
# ===                  .gitignore for Node.js Backend                   ===
# =========================================================================
# This file tells Git which files and directories to intentionally ignore.
# For more details, see the official documentation:
# https://git-scm.com/docs/gitignore

# -------------------------------------------------------------------------
# Dependencies
# -------------------------------------------------------------------------
# The node_modules directory contains all of your project's dependencies.
# This folder can become very large and should never be committed to Git.
# It is generated by running `npm install` or `yarn install`.
/node_modules

# -------------------------------------------------------------------------
# Logs
# -------------------------------------------------------------------------
# Log files are generated during runtime and are specific to an environment.
# They should not be version-controlled.
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
*.pid
*.seed
*.pid.lock

# -------------------------------------------------------------------------
# Environment Variables
# -------------------------------------------------------------------------
# Environment variables files contain secrets, API keys, and database
# credentials. This is a major security risk if committed.
# A `.env.example` file SHOULD be committed to show what variables are needed.
.env
.env.*
!.env.example
!.env.test.example

# -------------------------------------------------------------------------
# Build & Distribution Files
# -------------------------------------------------------------------------
# If you are using a transpiler like TypeScript, Babel, or a bundler,
# the compiled output should be ignored. Only commit the source code.
/dist
/build
/out
/.next
/.nuxt

# -------------------------------------------------------------------------
# Test & Coverage Reports
# -------------------------------------------------------------------------
# Test coverage reports are generated files and do not need to be in Git.
/coverage
/coverage.*
.nyc_output

# -------------------------------------------------------------------------
# Operating System & System Files
# -------------------------------------------------------------------------
# These files are specific to the user's operating system and do not
# belong in the project's repository.

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# -------------------------------------------------------------------------
# Editor & IDE Directories
# -------------------------------------------------------------------------
# User-specific settings for code editors.
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.idea
*.sublime-project
*.sublime-workspace

# -------------------------------------------------------------------------
# Miscellaneous
# -------------------------------------------------------------------------
# Other temporary files or caches generated by various tools.
.npm
.eslintcache
.tmp
.cache
*.tsbuildinfo

# -------------------------------------------------------------------------
# Uploads
# -------------------------------------------------------------------------
# User-uploaded files should not be committed to Git
/uploads


# =========================================================================
# A NOTE ON LOCK FILES (package-lock.json, yarn.lock)
# =========================================================================
# For applications (like this backend), you SHOULD commit the lock file
# (e.g., package-lock.json) to ensure that all developers and your
# deployment environment use the exact same dependency versions.
# That is why lock files are NOT listed in this .gitignore file.
# =========================================================================